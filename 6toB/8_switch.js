// MIRAR LOS VIDEOS PRIMERO!! Despues vuelvan a probar aca

let operacion = "&&"; // operador booleano y
switch (operacion) { // el switch compara la variable operacion y cada case con el operador de igualdad estricta ===
  case '+':
    // sumar
    break;
  case '-':
    // restar
    break;
  case '*':
    // multiplicar
    break;
  case '÷':
    // dividir (cuidado si el divisor es 0!)
    break;
  case '^':
    // potencia
    break;
  default:
    console.log("No era ninguna de las operaciones de arriba");
    break;
}